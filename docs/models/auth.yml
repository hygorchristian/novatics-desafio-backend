definitions:
  Auth:
    type: object
    properties:
      id:
        type: integer
      username:
        type: string
      email:
        type: string
      password:
        type: string
      token:
        type: string
      created_at:
        type: string
      updated_at:
        type: string
    required:
      - username
      - email
      - password
